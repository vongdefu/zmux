name: Publish Release

on:
  push:
    tags:
      - "v*"
    branches:
      - release

jobs:
  publish:
    permissions:
      contents: write # 允许创建 Release 并上传资产，同时允许推送到 gh-pages 分支
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
          - platform: "ubuntu-22.04"
          - platform: "windows-latest"

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. 初始化前端环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: "yarn"

      # 2. Linux 环境依赖安装
      - name: Install Linux dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      # 3. 初始化 Rust 环境
      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      # 4. 安装前端依赖
      - name: Install frontend dependencies
        run: yarn install

      # 5. 使用 Tauri 官方 Action 执行构建并发布
      # ⚠️ 注意：tauri-action 内部会自动替你执行一次 `yarn build`（对应你配置的 beforeBuildCommand）
      # 这意味着此时 dist 目录里的网页产物其实已经由 tauri-action 顺便生成好了！
      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: app-v__VERSION__
          releaseName: "App v__VERSION__"
          releaseBody: "See the assets to download this version and install."
          generateReleaseNotes: true
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}

      # 6. 【精简合并】仅在 Ubuntu 环境下，顺便将已经构建好的 dist 目录推送到 gh-pages 分支
      - name: Deploy Web to GitHub Pages (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04' # 👈 关键：加这个判断，确保只有一台机器去提交网页，绝不冲突
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: dist
